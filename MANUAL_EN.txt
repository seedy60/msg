===============================================================================
                       MUSIC SEPARATOR - USER MANUAL                       
                       ENGLISH MANUAL (EN) - v1.8
===============================================================================

Welcome to Music separator, a powerful AI-driven audio separation tool based on 
the state-of-the-art Roformer and MDX architectures. This application allows 
you to isolate vocals, instruments, and specific elements with studio quality.

OFFICIAL DOWNLOAD:
You can always download the latest version of Music separator from:
https://github.com/GianlucaApollaro/Music-Separator-GUI/releases/latest

1. INSTALLATION
-------------------------------------------------------------------------------
WINDOWS:
- Download the version suitable for your system (.7z for CPU or GPU).
- Extract the archive and launch 'Music separator.exe' (GPU version)
  or 'Music separator CPU.exe' (CPU version) directly.
- If installing from source:
  - Run 'install.bat' (or 'install_cpu.bat') to install dependencies.
  - Use 'run.bat' (or 'run_Cpu.bat') to launch the application.

macOS (Apple Silicon M1/M2/M3/M4):
- Download the Mac version (.zip) and extract the archive.
- Move the resulting folder to /Applications (e.g. /Applications/Music Separator).
- Right-click 'Music separator.app' and choose 'Open'.
- NOTE: FFmpeg is included in the package. If you face permission issues, 
  refer to the README for 'xattr' command instructions.

2. WHAT'S NEW in v1.8
-------------------------------------------------------------------------------
- Branched & Tree-Structured Custom Presets (Fork / Tree Pipelines):
  Custom presets now support non-linear separation chains. Starting from step 2,
  you can select any stem generated in earlier passes (or the original input)
  as your audio source, stop a branch with 'None (end of this branch)', and
  independently save and rename stems produced at any step.
- Enhanced VoiceOver Accessibility on macOS: Replaced all fixed dropdown controls
  with native wx.Choice (NSPopUpButton) widgets, providing seamless screen reader
  reading and navigation.
- Automatic Model Name Resolution: Prevents unsupported model file errors when
  saving custom presets using descriptive catalog titles.
- Audio Normalization & Auto-Updater Stability: Fixed UnboundLocalError during
  -0.1 dBFS normalization and eliminated extraction loops with wrapped 7z updates.

WHAT'S NEW in v1.7:
- Command-Line Interface (CLI / Headless): You can now use Music Separator directly
  from the terminal or command prompt for automated pipelines, DAW scripts, and
  headless servers without launching the graphical user interface.
- Preset Import and Export: Easily backup, share, and restore your custom presets
  via JSON files directly from the GUI or the command line.
- 5-Pass Ultimate Stems + Drum Split Preset: Advanced automated pipeline that extracts
  15 isolated tracks (including Bowed Strings, lead/backing vocals, bass, guitar,
  piano, and full drum kit separation) with a calibrated full instrumental mix.
- New Specialized AI Models: Integrated Bowed Strings BS-Roformer, Male/Female Vocal
  Duet MelBand Roformer, and Lead Synth BS-Roformer models.
- MVSep Mega 53 Stems Model Support: Isolate up to 53 individual musical instruments in a single pass.
- Audio Bit Depth & Bitrate Selection: Full support for 16-bit, 24-bit, and 32-bit
  Float exports for WAV, 16-bit and 24-bit for FLAC, and custom bitrates
  (320k, 256k, 192k, 128k) for MP3 via an accessible dynamic selector.
- Configurable Silent Stem Threshold: Custom silence threshold from -20 dB down to -80 dB.
- Runtime Monkey-Patches Refactoring: Brand-new modular runtime engine preventing
  memory leaks and avoiding wrapper stacking across consecutive runs.
- Enhanced Efficiency & Robustness: Atomic file configuration writes, smart model
  catalog caching, and strict download integrity verification.

WHAT'S NEW in v1.6:
- User-Customizable Presets: Create multi-pass chained presets directly from GUI.
- Downloaded Models Grouping (Offline).
- VoiceOver Accessibility on macOS.

WHAT'S NEW in v1.5:
- Enable Preview: Cuts and separates a 30-second portion of the audio (first or
  final 30s) in a dedicated folder named 'previews'.
- Bandit & SCNet Support: Dynamic loading and mock-dependencies for ZFTurbo models.
- Auto-Updater: Automatic update checks at startup or via the Help menu.

3. BASIC USAGE (SINGLE MODEL)
-------------------------------------------------------------------------------
1. Select your input file (WAV, FLAC, MP3, MP4, MKV, etc.).
2. Choose an Output Directory.
3. Select a Model from the list picker.
   - On Windows/Linux, models are shown in a categorized tree with a search bar.
   - On macOS, models are listed in an accessible ListBox.
   - Downloaded models appear at the top under "Downloaded Models (Offline)".
4. Select the output format (WAV, FLAC, or MP3) and the target quality:
   - For WAV: 16-bit, 24-bit, or 32-bit Float (studio quality).
   - For FLAC: 16-bit or 24-bit (high-resolution lossless).
   - For MP3: 320 kbps, 256 kbps, 192 kbps, or 128 kbps.
5. Click "Start Separation".

4. NATIVE ENSEMBLE (MIXING MODELS)
-------------------------------------------------------------------------------
Tick the "Ensemble" checkbox to select TWO different models. The application 
will process the file with both and fuse the results.
- Ensemble Algorithm: Choose how to mix the tracks (Average, Min, Max, Median).
- Recommended: "avg_wave" (Average) for the most natural sound.

5. CHAINED ENSEMBLE PRESETS & CUSTOM PRESETS
-------------------------------------------------------------------------------
The "Preset" dropdown allows you to run "Chained" processes where the output 
of one model is automatically fed into another.

CREATING CUSTOM PRESETS (LINEAR AND BRANCHED TREE PIPELINES):
- Click the "Create Preset" button next to the preset dropdown to open the Creator Dialog.
- Set a Name and choose between "Single Model" or "Chained Models".
- Linear or Branched Workflows:
  * Audio Source: Starting from Step 2, you can choose which audio track feeds into
    each step (leave default for standard sequential linear flow, or select any stem
    generated in previous passes or the original input audio to branch out).
  * Pass Stem: Select which stem forwards into the next pass, or choose 'None (end of this branch)'
    to terminate an intermediate branch without passing audio downstream.
  * Stems to Keep and Rename: For each pass, select which stems to save to disk and
    specify custom filename suffixes (e.g. _Lead_Dry).
- Save your preset. It will instantly appear in the preset dropdown and handle audio routing automatically.
- To delete, select your custom preset and click the "X" button next to it.

IMPORTING & EXPORTING PRESETS:
- Click "Export Presets" (or File menu -> "Export Presets...") to save your presets to a portable JSON file.
- Click "Import Presets" (or File menu -> "Import Presets...") to load presets from backups or other computers.
- Also available via CLI using `--export-presets [file.json]` and `--import-presets <file.json>`.

AVAILABLE BUILT-IN PRESETS:
- Vocal Split: Separates Vocals/Inst, then splits Vocals into Lead and Backing.
- Vocal De-Reverb: Separates Vocals, then removes reverb/echo from the voice.
- Ultimate Stems (7 Tracks): High-quality Vocals and Instrumental extraction, 
  then splits Instrumental into Bass, Drums, Piano, Guitar, and Other.
- Ultimate Stems + Drum Split: A 5-pass preset that extracts full Instrumental
  (_Instrumental, with strings included), Bowed Strings (_Strings), Lead Vocals,
  Backing Vocals, Bass, Piano, Guitar, Other, and splits drums into individual
  components (Kick, Snare, Hi-Hat, Toms, Cymbals, etc., for 15 tracks total).
- Guitar Extraction: Specialized preset to isolate guitars from the track.
- Male/Female Split: Specifically isolates male vs. female voices.
- Live Restore: Extracts Crowd/Applause, then cleans the remaining audio.
- RVC Vocal Prep: Extracts clean Lead vocals for RVC voice cloning.
- Only Drums: Isolates only the drum kit, removing all other instruments.
- Only Drums + Backing: Extracts the drums (_Drums) and merges the rest of the
  song into one backing track (_No_Drums).

6. OUTPUT OPTIONS & CLEANUP
-------------------------------------------------------------------------------
- Save in subfolder: Creates a folder named after the song. If disabled, 
  files are saved directly in the output directory.
- Delete silent stems: If enabled, the program deletes generated stems that are 
  quieter than the configured threshold (-20 dB to -80 dB in 5 dB steps; default: -50 dB).
- Play Stem: Click to play the newly created stems directly in your default 
  system media player.

7. CHUNKING (LONG FILES / OUT-OF-MEMORY PREVENTION)
-------------------------------------------------------------------------------
If you experience crashes with long files or have low VRAM:
- Check "Enable Chunking" and select a chunk size (e.g., 2 min).

8. BATCH PROCESSING (FILES AND FOLDERS)
-------------------------------------------------------------------------------
Process multiple files automatically by dragging them or using the "Browse" 
and "Browse Folder" buttons.

9. TECHNICAL NOTES AND SUFFIXES
-------------------------------------------------------------------------------
Output files will have clear suffixes such as:
_Vocals, _Instrumental, _Lead, _Backing, _DeReverb, _Other, _Extra, _Drums, _No_Drums.

10. COMMAND-LINE INTERFACE (CLI / HEADLESS MODE)
-------------------------------------------------------------------------------
Starting with version 1.7, Music Separator includes a full-featured command-line
interface to automate audio stem separations without opening the graphical window.

BASIC SYNTAX:
  music-separator -i <audio_file(s)> [options]

KEY ARGUMENTS:
  -i, --input <files...>      One or more audio files, folders, or glob patterns (e.g. *.wav).
  -o, --output-dir <dir>      Output folder where stems will be saved (default: ./output).
  -m, --model <name>          Model friendly name, alias, or substring (does not require .ckpt/.onnx extensions, e.g. "Viperx-1297" or "Inst_HQ_5").
  -p, --preset <name>         Name or key of a preset (e.g. preset_vocal_split or "Denoise + Vocals").
  -r, --recursive             Recursively scan subfolders when input is a directory.
  --format {WAV,FLAC,MP3}     Output audio format (default: WAV).
  --bit-depth {16,24,32}      Bit depth for WAV (16, 24, 32 Float) and FLAC (16, 24).
  --bitrate {320k,256k,192k}  Bitrate for MP3 (default: 320k).
  --gpu                       Force GPU acceleration (CUDA / Apple MPS).
  --cpu                       Force CPU-only processing.
  --no-subfolder              Save stems directly in output folder without creating a subfolder per track.
  --clean-names               Strip leading track numbers (e.g. '01 - ') from folder names.
  --delete-silent-stems       Automatically delete generated stems that contain only silence.
  --silent-stem-threshold <dB> Threshold in dB below which a stem is considered silence (e.g. -50, -45, -60; default: -50 dB).
  --chunk-duration <seconds>  Process audio in chunks of N seconds (ideal for long audio / low VRAM).
  --preview {first,final}     Process only a 30-second preview (first or final 30s).
  -l, --list-models           List all available models in the catalog and their download status.
  --list-presets              List all available built-in and custom presets.
  --download-model <model>    Download a model to the local catalog and exit.
  -q, --quiet                 Minimize console output (only print errors and results).
  -v, --version               Display application version and exit.

WINDOWS EXAMPLES:
  - Model separation:
    "Music separator.exe" -i song.wav -m "BS-Roformer-SW.ckpt" -o "./output"
  - Using a preset:
    "Music separator.exe" -i song.wav --preset "preset_vocal_split"
  - Processing an entire folder (recursive):
    "Music separator.exe" -i "D:\Music\Album" -r --preset "preset_vocal_split"
  - Inspecting models and presets:
    "Music separator.exe" --list-models
    "Music separator.exe" --list-presets

macOS EXAMPLES (TERMINAL):
  - Direct execution:
    "/Applications/Music Separator/Music separator.app/Contents/MacOS/Music separator" -i song.wav -m "BS-Roformer-SW.ckpt"
  - Quick alias in ~/.zshrc:
    alias music-separator='"/Applications/Music Separator/Music separator.app/Contents/MacOS/Music separator"'
    music-separator -i song.wav --preset "preset_vocal_split"

===============================================================================
